---
name: bs:backlog
description: "Show/manage project backlog with value-based prioritization"
argument-hint: '[--add "item" | --stats | --next]'
category: project
model: haiku
---

# /bs:backlog — $ARGUMENTS

Invoke the `backlog` skill. Pass `$ARGUMENTS` unchanged.
