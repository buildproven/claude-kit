name: Credential History Scan

on:
  # This workflow is deliberately evaluated from the trusted base branch.
  # It fetches PR commits as data for Gitleaks, but never checks out or
  # executes PR-controlled workflow or repository code.
  pull_request_target:
    branches: [main]
    # Do not path-filter this workflow: credentials can be committed in any
    # file, including documentation, configuration, or a transient file later
    # deleted in the same pull request.
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  credential-history-scan:
    name: credential-history-scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
          ref: ${{ github.event.pull_request.base.sha }}

      - name: Install pinned Gitleaks
        run: |
          set -euo pipefail
          version=8.30.1
          archive="gitleaks_${version}_linux_x64.tar.gz"
          curl --fail --silent --show-error --location --remote-name \
            "https://github.com/gitleaks/gitleaks/releases/download/v${version}/${archive}"
          curl --fail --silent --show-error --location --remote-name \
            "https://github.com/gitleaks/gitleaks/releases/download/v${version}/gitleaks_${version}_checksums.txt"
          matches=$(grep -c " ${archive}$" "gitleaks_${version}_checksums.txt" || true)
          test "$matches" -eq 1
          grep " ${archive}$" "gitleaks_${version}_checksums.txt" | sha256sum --check --strict
          tar --extract --gzip --file "$archive" --to-stdout gitleaks > "$RUNNER_TEMP/gitleaks"
          chmod +x "$RUNNER_TEMP/gitleaks"
          "$RUNNER_TEMP/gitleaks" version

      - name: Scan introduced history with trusted policy
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]
          git fetch --no-tags origin "$HEAD_SHA"
          # Once the policy exists, it comes from the trusted base so a pull
          # request cannot weaken its own detection rules while adding a
          # credential. This first bootstrap PR has no base policy yet, so
          # deliberately use Gitleaks' built-in defaults instead of trusting
          # the PR-controlled new config.
          config_args=()
          if git cat-file -e "$BASE_SHA:.gitleaks.toml" 2>/dev/null; then
            git show "$BASE_SHA:.gitleaks.toml" > "$RUNNER_TEMP/.gitleaks.toml"
            config_args=(--config "$RUNNER_TEMP/.gitleaks.toml")
          else
            # Do not let Gitleaks auto-discover a configuration in the fetched
            # PR tree during the first policy bootstrap.
            printf '[extend]\nuseDefault = true\n' > "$RUNNER_TEMP/.gitleaks.toml"
            config_args=(--config "$RUNNER_TEMP/.gitleaks.toml")
          fi
          : > "$RUNNER_TEMP/.gitleaksignore"
          "$RUNNER_TEMP/gitleaks" git \
            "${config_args[@]}" \
            --gitleaks-ignore-path "$RUNNER_TEMP/.gitleaksignore" \
            --ignore-gitleaks-allow \
            --max-archive-depth 1 \
            --redact \
            --log-opts="--diff-merges=first-parent -a ${BASE_SHA}..${HEAD_SHA}"
